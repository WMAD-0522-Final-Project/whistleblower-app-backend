export enum ClaimStatus {
  Unhandled = 'unHandled',
  InProcess = 'inProcess',
  Done = 'done',
  Archived = 'archived',
}

export enum UserRoleOption {
  General = 'general',
  Admin = 'admin',
  SuperAdmin = 'superAdmin',
}
